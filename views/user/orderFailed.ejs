<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose Payment Method</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="css/elegant-icons.css" type="text/css">
    <link rel="stylesheet" href="css/jquery-ui.min.css" type="text/css">
    <link rel="stylesheet" href="css/magnific-popup.css" type="text/css">
    <link rel="stylesheet" href="css/owl.carousel.min.css" type="text/css">
    <link rel="stylesheet" href="css/slicknav.min.css" type="text/css">
    <link rel="stylesheet" href="css/style.css" type="text/css">
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
        }

        .container {
            margin-top: 30px;
        }

        h1 {
            font-size: 2rem;
            font-weight: bold;
            text-align: center;
            margin-bottom: 10px;
        }

        p {
            text-align: center;
            color: #e5b4cc;
        }

        /* Stepper CSS */
        .stepper {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 30px;
        }

        .step {
            display: flex;
            align-items: center;
            padding: 0 10px;
        }

        .step .circle {
            height: 30px;
            width: 30px;
            border-radius: 50%;
            background-color: #ff007f;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-weight: bold;
        }

        .step .line {
            width: 50px;
            height: 3px;
            background-color: #cccccc;
            margin: 0 5px;
        }

        .step.active .line {
            background-color: #ff007f;
        }

        .step.active .circle {
            background-color: #ff007f;
        }

        .step.completed .circle {
            background-color: #ff007f;
        }

        .payment-method-card {
            border: 1px solid #ebebeb;
            border-radius: 8px;
            padding: 20px;
            background-color: #fff;
            margin-bottom: 20px;
        }

        .payment-method-card input[type="radio"] {
            margin-right: 10px;
        }

        .btn-primary {
            background-color: #ff007f;
            border-color: #ff007f;
        }

        .btn-primary:hover {
            background-color: #e6006c;
            border-color: #e6006c;
        }

        /* Custom style for SweetAlert2 success icon */
        .swal2-success .swal2-success-ring {
            border-color: #ff007f !important; /* Pink color for the outer ring */
        }

        .swal2-success .swal2-success-line {
            background-color: #ff007f !important; /* Pink color for the tick mark */
        }
        
        .success-icon {
            font-size: 50px; /* Adjust size as needed */
            color: #ff007f; /* Match your theme color */
            margin-bottom: 20px; /* Space between icon and text */
        }
    </style>
    <!-- SweetAlert2 CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/sweetalert2/11.7.9/sweetalert2.min.css">
</head>
<body>
    <div class="container">
        <!-- Stepper -->
        <div class="stepper">
            <div class="step completed">
                <div class="circle">✓</div>
                <span>Sign Up</span>
                <div class="line active"></div>
            </div>
            <div class="step completed">
                <div class="circle">✓</div>
                <span>Address</span>
                <div class="line active"></div>
            </div>
            <div class="step active">
                <div class="circle">3</div>
                <span>Payment</span>
            </div>
        </div>

        <div style="text-align: center;padding: 60px;width: 500px;margin-left: 310px;background: #ffffff;border-color: #000000;border-radius: 5px;margin-top: 50px;" class="checkout__order">
                <!-- <i class="fas fa-check-circle"></i> -->
                 <img style="width: 200px;height: 200px;" src="/imgs/success.png" alt="">
            <h2>Your Order is Successfully placed</h2>
            <br>
            <a href="/confirmOrder2?orderId=<%= orderId %>">
                <button style="background-color: #000000;color: #ebebeb;height: 40px;border-radius: 5px;" id="rzp-button">Try Again</button>
            </a>
        </div>
    </div>

    <!-- SweetAlert2 JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert2/11.7.9/sweetalert2.all.min.js"></script>
    <!-- Bootstrap JS CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
   
</body>
</html>
