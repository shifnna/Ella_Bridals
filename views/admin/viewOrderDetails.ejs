<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Womenz Store</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            height: 100%;
        }
        .container {
            display: flex;
            min-height: 100vh;
        }
        .sidebar {
            background-color: #253234;
            min-height: 100vh;
            width: 250px;
        }
        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }
        .sidebar ul li {
            margin: 0;
        }
        .sidebar ul li a {
            display: flex;
            align-items: center;
            padding: 15px;
            text-decoration: none;
            color: #e7dddd;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .sidebar ul li a:hover {
            background-color: #6f898d;
            color: white;
            text-decoration: none;
        }
        .sidebar ul li a i {
            margin-right: 10px;
        }
        .main-content {
            flex: 1;
            padding: 20px;
        }
        .top-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .breadcrumb {
            font-size: 18px;
        }
        .profile-pic {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }
        .dashboard {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .table-box {
            margin-top: 20px;
        }
        .table-box input {
            padding: 10px;
            width: 100%;
            box-sizing: border-box;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .action-button {
            background-color: red;
            color: white;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
        }
        .action-button:hover {
            background-color: darkred;
        }
        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 20px;
            padding: 0; 
            list-style: none;
        }
        .page-item {
            margin: 0 5px;
            list-style: none;
            margin-right: 70px;
        }
        .page-link {
            padding: 10px 15px;
            text-decoration: none;
            color: #007bff;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: #fff;
        }
        .page-link:hover {
            background-color: #e9ecef;
        }
        .active .page-link {
            background-color: #007bff;
            color: #fff;
        }

        /* New CSS for the order card and alignment */
        .order-card-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid #ebebeb;
            background-color: #f9f9f9;
            padding: 16px;
            border-radius: 8px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 16px;
        }
        .order-card {
            display: flex;
            align-items: center;
            flex: 1;
        }
        .order-card img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            margin-right: 16px;
        }
        .order-card .details {
            flex: 1;
        }
        .order-card .details h6,
        .order-card .details p {
            margin: 0;
        }
        .pink-div {
            background-color: pink;
            height: 50px;
            width: 50px;
            border-radius: 8px;
            padding-left: 200px;
        }
        .pink2-div {
            background-color: pink;
            height: 50px;
            width: 50px;
            border-radius: 8px;
            padding-left: 200px;
            margin-left: 80px;
        }
        .green-div {
            background-color: lightgreen;
            height: 50px;
            width: 50px;
            border-radius: 8px;
            padding-right: 200px;
            margin-left: 100px;
        }
        .center-div {
            display: flex;
            justify-content: center;
            align-items: center;
            flex: 1;
        }

        /* Table Styling */
        .order-table {
    width: 800px; /* Set the table width to 800px */
    border-collapse: collapse;
    background-color: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}
        .order-table td {
            padding: 15px;
            border-bottom: 1px solid #ddd;
        }
        .order-table img {
            width: 80px;
            height: 80px;
            object-fit: cover;
        }

        .address-container {
    width: 300px; /* Set the address container width to 300px */
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    margin-left: 20px; /* Space between the table and address container */
    margin-top: 20px;
}
.dashboard {
    display: flex; /* Use flexbox to align items side by side */
    justify-content: space-between; /* Ensure there is space between items */
    align-items: flex-start; /* Align items to the start of the flex container */
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
   
}

.dashboard2 {
    display: flex;
    margin-top: 5px;
    flex-direction: column; /* Ensures elements stack vertically */
    align-items: flex-start; /* Align items to the start of the flex container */
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.dashboard h3 {
    margin: 0;
    font-size: 18px;
    margin-bottom: 10px; /* Adds space between the heading and the dropdown */
}

.dashboard select {
    padding: 10px;
    border-radius: 4px;
    border: 1px solid #ddd;
    width: 200px; /* Adjust the width of the dropdown as needed */
}

    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <div class="sidebar">
            <h1 style="margin-left: 18px;color: rgb(226, 222, 222);">Womenz Store</h1>
            <ul>
                <li><a href="/admin/dashboard"><i class="fas fa-home"></i> Dashboard</a></li>
                <li><a href="/admin/customers"><i class="fas fa-users"></i> Customers</a></li>
                <li><a href="/admin/category"><i class="fas fa-list"></i> Category</a></li>
                <li><a href="/admin/brands"><i class="fas fa-star"></i> Brands</a></li>
                <li><a href="/admin/addProducts"><i class="fas fa-plus"></i> Add product</a></li>
                <li><a href="/admin/products"><i class="fas fa-box"></i> Products</a></li>
                <li><a href="/admin/coupon"><i class="fas fa-tags"></i> Coupon</a></li>
                <li><a href="/admin/banner"><i class="fas fa-image"></i> Banner</a></li>
                <li><a href="/admin/order"><i class="fas fa-receipt"></i> Orders</a></li> 
                <li><a href="/admin/offers"><i class="fas fa-gift"></i> Offers</a></li> 
                <li><a href="/admin/logout" id="logout-btn"><i class="fas fa-sign-out-alt"></i> Logout</a></li>              

            </ul>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <!-- Top Header -->
            <div class="top-header">
                <h2>Orders</h2>
                <div class="profile">
                    <img src="/img/avatar2.png" alt="Profile Picture" class="profile-pic">
                </div>
            </div>

            <!-- Order Table and Address Details -->
            <div class="dashboard">
                <!-- Table for Order Details -->
                <div class="table-box">
                    <table class="order-table">
                        <tbody>
                            <% order.products.forEach(function(product) { %>
                            <tr>
                                <td><%=product.name%></td>
                                <td><%=order.status%></td>
                                <td><%= order.orderDate.toLocaleDateString('en-US', { weekday: 'short', year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                                <td><img  src="/imgs/re-images/<%=product.productImage[0] %>" alt="Product Image"></td>
                            </tr>

                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <!-- Address Details Container -->
                <div class="address-container">
                    <h3>Customer Address</h3>
                    <p><strong>Name:</strong> <%= order.address[0].username %></p>
                    <p><strong>Address:</strong> <%= order.address[0].address %>, <%= order.address[0].pincode %>,  <%= order.address[0].city %>,  <%= order.address[0].state %></p>
                    <p><strong>Phone:</strong>  <%= order.address[0].mobile %></p>
                    <p><strong>Email:</strong> <%= user.email%></p>
                </div>
            </div>
            <form action="/admin/changeStatus/?orderId=<%=order._id%>" method="post">
                <div class="dashboard2">
                    <h3>Order Status</h3>
                    <select id="order-status" name="status">
                        <% if (order.status === "Cancelled") { %>
                            <option style="color: orangered;" value="Cancelled">Cancelled</option>
                        <% } else { %>
                            <% if (order.status !== "Delivered") { %>
                                <option style="color: #007bff;" value="OrderConfirmed">Order Confirmed</option>
                                <option style="color: orangered;" value="Cancelled">Cancelled</option>
                                <option style="color: #007bff;" value="shipped">Shipped</option>
                                <option style="color: #007bff;" value="Return">Return</option>
                            <% } %>
                            <option style="color: green;" value="delivered">Delivered</option>
                        <% } %>
                    </select>            
                </div>
            <div>
                <button style="margin: 15px;padding: 10px;background: green;color: #ebebeb;border: none;border-radius: 5px;" id="update-status">Update Status</button>
            </div>
        </form>
        </div>
    </div>
</body>
</html>
